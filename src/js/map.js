// src/js/map.js
// Stub (não quebra o app caso o mapa ainda não esteja implementado)
(function () {
  if (!window.SumaumaMap) {
    window.SumaumaMap = {
      init: function () {
        // No-op: o mapa real será implementado depois
        // Mantém o app funcionando (home + rotas)
      }
    };
  }
})();
